# Node
node_modules/

# Local data — config.json contains credentials, so it stays ignored.
# store.json is deliberately tracked in this repo (regulatory-monitor-100 only)
# so Render deploys start with the already-processed corpus instead of empty.
data/*
!data/store.json

# Local environment overrides
.env
.env.local

# Dev utility scripts (safe to delete)
debug_ibbi.js
probe_all.js
debug.js
sample.js

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Server run logs (local debugging only)
server*.log

# Unrelated scratch project accidentally nested in this folder
AI-assistent-sunhacks/
